apiVersion: v1
kind: Service
metadata:
  name: {{ include "nullplatform-agent.fullname" . }}
  labels:
    {{- include "nullplatform-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nullplatform-agent.selectorLabels" . | nindent 4 }}
